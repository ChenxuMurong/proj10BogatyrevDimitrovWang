class Main extends List {
    random x = 1;

    void main(){
        var x = super.x.a(Sys);
        var y = (Sys) x;
        cast(int,(1+1)*5);
        var y = (super.yo = this.x * 5);
        for(; ; ){
            (String) x;
            x = (Object) x;
            Sys.yes();
        }

    }
}